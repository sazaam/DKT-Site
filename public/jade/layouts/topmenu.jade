#topbar.topbar.navbar.navbar-custom.navbar-fixed-top


  each topsection in topsections
    //- h1= topsection.id
    - var prefix = 'DKT__' ;
    - topsection.cleanname = t( prefix + topsection.name + '.name') ;
    - topsection.surname = t( prefix + topsection.name + '.surname')
    - topsection.cleanlink = '/#/' + lang + topsection.path ;
    //- h2= topsection.cleanname
    //- h2= topsection.surname
    //- h2= topsection.cleanlink
    if(topsection.children)
      each ch in topsection.children
        - ch.cleanname = t( prefix + topsection.name + '.' + topsection.name + '.' + ch.name + '.name') ;
        - ch.surname = t( prefix + topsection.name + '.' + topsection.name + '.' + ch.name + '.surname') ;
        - ch.cleanlink = '/#/' + lang + ch.path ;
        //- h1= ch.cleanname
        //- h1= ch.surname
        //- h1= ch.cleanlink
    
  //- MAIN NAV SECTION LINKS
  ol.sectionsnav.flowvisible.uppercase
    - var other = [].concat(topsections) ;
    - var home = other.shift() ;
    
    //-  HOME SPECIAL CASE
    li.home.floatL(id= "global_" + 'home', class= "global_" + 'home')
      a.block.pad.inconsolata(href= '/#/' + lang + '/home/')
        span= t('DKT')
    
    //-  OTHERS CASE
    each navsection in other
      - var name = navsection.name ;
      li.others.floatL.rel(id= "global_" + name, class= "global_" + name)
        a.block.pad.Hpad.inconsolata(href= '/#/' + lang + '/' + name + '/')
          span= name
        if(navsection.children.length)
          ol.subnav.abs.txtL.left0.round.right.flowhidden(style="top:4em;width:400px")
            each navchild, i in navsection.children
              - var chname = navchild.cleanname ;
              li.sub(class="navmenu_" + navchild.name )
                a.block.padXXSm.Hpad.inconsolata(href= '/#/' + lang + '/' + name + '/' + navchild.name + '/')
                  span= chname

  //- LAGUAGE BAR
  ul#langnav.langnav.context.floatR.mar.VpadXXXSm
    each language in langs
      - var active = language == lang ? ' active ' : '' ;
      li.floatL(id= "lang_" + language, class= "lang " + active + "lang_" + language)
        a.block.HpadXSm.inconsolata.round.both(href='?lng=' + language, lang=language)
          span= language

