//- ----------------------- START  => Extends

extends ./page

//- ----------------------- END : Extends

block content
  - var target_sections = [].concat([db_sections[0]]).concat([db_sections[2]]).concat([db_sections[1]]).concat(db_sections[1].children) ;
  
  //- script. 
    console.log(!{JSON.stringify(locals.__dirname)})
    console.log(!{JSON.stringify(locals.filename)})

  //- HOME SLIDESHOW
  .extra.rel.fullH.context
    
    +slideshow()




  block flow
    
    //- HOME ARTICLE BLOCKS
    each article in getComponentsByTypename(section.post.bracket, 'ComponentModularArticle')
      +article(article, params)
    
    
    //- PATCHWORK
    - var target_sections = [].concat(db_sections[1].children) ;
    +patchwork()
    
    +footer()

    